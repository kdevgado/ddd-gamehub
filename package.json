{
  "name": "ddd-gamehub",
  "version": "1.0.4",
  "description": "Pass-the-phone party games and live online game rooms.",
  "author": "DDD Game Hub",
  "private": true,
  "type": "module",
  "main": "desktop/main.cjs",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "android:sync": "npm run build && cap sync android",
    "android:open": "npm run android:sync && cap open android",
    "android:apk": "npm run android:sync && node scripts/build-android.cjs",
    "android:apk:debug": "npm run android:sync && node scripts/build-android.cjs --debug",
    "desktop:run": "npm run build && electron .",
    "desktop:installer": "npm run build && electron-builder --win nsis --publish never"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "firebase": "^12.17.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@vitejs/plugin-react": "^5.2.0",
    "electron": "^43.1.0",
    "electron-builder": "^26.15.3",
    "vite": "^6.4.3"
  },
  "build": {
    "appId": "com.ddd.gamehub",
    "productName": "DDD Game Hub",
    "asar": true,
    "directories": {
      "app": "desktop",
      "output": "release/windows"
    },
    "files": [
      "main.cjs",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "dist",
        "to": "dist"
      }
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "public/icons/imposter/custom.png",
      "artifactName": "DDD-Game-Hub-Windows-Setup.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "DDD Game Hub"
    }
  }
}
